.DS_Store
node_modules/
content/slides.json.bak

# Data pipeline: transient WID fetch state (see data/README.md)
data/raw/wid/temp_country_data/
data/raw/wid/fetch_progress.json
data/scripts/__pycache__/

# Raw screen recordings: straight off a Retina display these run to 100 MB+,
# which GitHub REJECTS outright (100 MiB hard limit per file). Compress and
# convert before committing, and give the result a real name — then it is not
# matched by this rule.
content/images/Screen Recording*.mov
